# 2026-09-12 项目维护实施记录

## 已实施

- 从四个远程历史提交恢复缺失日报，保留原文和期数；当前主题索引从 48 期补齐为 52 期。
- 把迁移前 24 个文件保存为带 SHA-256 的快照，原分周记录保留；生成统一论文表、兼容视图、来源记录、冲突表与去重集。
- 449 条论文身份合并到统一表，342 个 arXiv ID 进入历史已入选去重集。相较原 330 个唯一 ID，增加 9 月 11 日遗漏的 1 个及历史日报已入选但旧去重表未记的 11 个。
- 121 条记录带有历史正式发表/接收声明；这不是新一轮在线验证结果。8 项出版字段冲突仍待复核。3 条记录缺少可提取标题，327 条缺少结构化完整作者列表，已保留 unknown/null；原作者陈述与来源可查询。
- 新输入使用有来源的事件；评分区分 daily-v1 和 published-v2；状态推进、纠错与历史来源独立保留。
- 查询/候选/决策/窗口采用结构化运行记录，精确计数由程序计算；失败及降级不能推进成功覆盖位置。历史估计检索量未转造精确记录。
- README 改为最新入口，生成全部期刊/日报索引、可搜索论文目录及重点论文对照；未知实验细节不推断补全。
- 发布工具只暂存明确路径，验证工作区和真正暂存的快照，执行两次规定推送并验证远程 main 包含提交且文件 blob 一致。拒绝自动 rebase 重试与夹带无关 HTML。
- 增加 GitHub Actions 校验工作流与事件/运行 JSON Schema；CLI 使用标准库执行实际约束，测试未依赖外部 Schema 引擎。

## 验证范围

本地标准库测试覆盖身份归并、歧义隔离、原始证据保留、发表证据边界、评分与查询契约、窗口推进、生成幂等性，
以及临时 bare Git 远程下的推送冲突、并发改写、部分推送失败、暂存遗漏依赖和范围保护。
检查还核验全部 JSONL 语法、历史快照、补档文件哈希、本地 Markdown 链接和生成视图一致性。
实际测试数量与发布提交见此次执行输出；本报告不预先宣称远程 Actions 已运行成功。

## 尚未获得的外部证据

外部 Claude/GPT 调度器的当前配置和首次执行结果未核验，也没有对它们执行写入。
[提示词指纹](../../metadata/prompt_manifest.json)保存仓库内版本，外部部署状态保持 unverified/null；
应按[操作手册](../OPERATIONS.md)完成配置回读和首次真实执行验证。

未重新联网核验全部历史论文，未将历史声明升级为本次已核验事实。待办应从
[数据质量报告](DATA_QUALITY.md)中的具体字段冲突与缺失项开始，以有来源的新事件逐条补充。
